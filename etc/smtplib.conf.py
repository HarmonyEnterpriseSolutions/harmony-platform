# If specified, `host' is the name of the remote host to which to connect.  
# If specified, `port' specifies the port to which to connect.
# By default, smtplib.SMTP_PORT is used.
# An SMTPConnectError is raised if the specified `host' doesn't respond correctly.
# If specified, `local_hostname` is used as the FQDN of the local host.
# By default, the local hostname is found using socket.getfqdn().

host='mail.local.wwm.com.ua'
